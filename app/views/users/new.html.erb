<h1>Log in</h1>

    <%= form_for(:sessions, url: login_path) do |f| %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :password %>
      <%= f.text_field :password %>

      <%= f.submit "Log in" %>
    <% end %>

    <p>New user?</p>
